<Page x:Class="Graduation.Pages.InfoPages.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Graduation.Pages.InfoPages"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:Wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="840" d:DesignWidth="1380"
      Title="StatisticsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <ui:Button x:Name="BackButton" Height="40" Width="120" Margin="20 0 0 0" Content="Назад" Click="BackButton_Click"/>
            <ComboBox x:Name="StatisticComboBox" Height="35" Width="450" Margin="20 0 0 0" SelectionChanged="StatisticComboBox_SelectionChanged"/>
        </StackPanel>
        <Wpf:PieChart x:Name="StatisticPieChart" Grid.Row="1" Height="600" Width="600" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Page>
